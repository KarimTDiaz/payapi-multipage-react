import styled from "styled-components";
import { COLORS, FONTS_SIZE } from "../../constants/variables";

const StyledMainTitle = styled.h1`
font-size:${FONTS_SIZE.m};
color: ${COLORS.secondary.textDark};
`

export { StyledMainTitle }